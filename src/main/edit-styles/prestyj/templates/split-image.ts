import type { EditStyleTemplate } from '../../shared/types';

export const splitImage: EditStyleTemplate = {
  archetype: 'split-image',
  zoomStyle: 'none',
  // Picker metadata matches the stable ordinary subtitle track.
  captionPosition: 'lower-third',
  hookTitleY: 220,
  rehookY: 220,
};
