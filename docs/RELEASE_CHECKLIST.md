# BatchClip 0.1.0 release checklist

## Redistribution-rights gate — BLOCKED

**Recorded 18 July 2026:** no BatchClip installer, DMG, AppImage, unpacked application, or updater payload may be distributed until every blocker in [`THIRD_PARTY_NOTICES.md`](../THIRD_PARTY_NOTICES.md) is resolved.

The completed audit covers 22 resource TTFs, the `@fontsource/inter` webfonts, 6 cleared procedural resource SFX, 3 unresolved renderer UI sounds, an empty music library, all 25 files under `build/`, the unresolved Media Master logo, Lucide icon content, and compiled production payloads selected by `package.json`/`package-lock.json`. Exact asset hashes and dependency versions are enforced by `scripts/audit-third-party-assets.mjs`.

### Recorded findings

- [x] Every current file under `resources/` and `build/` has a pinned audit hash.
- [x] Bundled source assets were scanned; the three UI sounds and Media Master logo were found and blocked for missing grants.
- [x] All 22 TTFs carry redistribution permission; OFL-1.1 and Apache-2.0 text and font-specific copyright/Reserved Font Name notices are preserved.
- [x] The six `resources/sfx/*.mp3` files are project-owned procedural works recorded in repository history and cleared for redistribution.
- [x] No music recording is currently bundled.
- [x] BatchClip build artwork is project-owned, generated by `scripts/generate-release-art.py`, and covered by the repository MIT license.
- [x] Native/compiled npm payloads were inventoried across macOS and Windows target packages.
- [x] `THIRD_PARTY_NOTICES.md` is configured as an `extraResource` and future payload assertions require an exact copy.
- [x] `beforePack` and both platform release scripts invoke the non-bypassable redistribution gate.
- [ ] Replace the `ffmpeg-static` build configured with `--enable-nonfree`; preserve exact licenses, configuration, and corresponding source.
- [ ] Obtain redistribution terms and organization eligibility/company-license evidence for Remotion; replace its `--enable-nonfree` media payload.
- [ ] Add target-specific ffprobe license/source compliance materials or replace/remove `@ffprobe-installer`.
- [ ] Add LGPL, third-party notices, corresponding source, and relinking material for sharp-libvips or remove it.
- [ ] Preserve ONNX Runtime v1.26.0 `LICENSE` and `ThirdPartyNotices.txt`, verify DirectML/DXC terms, and exclude non-target binaries or remove it.
- [ ] Replace/remove the three renderer UI sounds unless a source and redistribution grant is recorded.
- [ ] Add written Media Master logo redistribution/trademark permission or remove the logo.
- [ ] Re-run the audit, update the notices and hashes, and obtain a passing release gate before distributing anything.

### Gate commands

```bash
npm run audit:third-party
npm run audit:third-party -- --release
```

The first command verifies audit integrity and reports the seven known blockers. The second command must fail today; `npm run release:mac`, `npm run release:win`, `npm run build:linux`, direct electron-builder packaging, and unpacked packaging are blocked through the same gate. A future release is eligible only when `--release` exits zero and this section records the replacement evidence.

## Repository release gate — PASS (distribution remains blocked)

**Recorded 18 July 2026 at source HEAD `0eb4697266eb0fe795d081eb69ae14b78ab9073d` on Node `v24.15.0` / npm `11.12.1`.**

The requested repository quality gate passes. This does **not** override the redistribution block above: no new release installer was built or approved for distribution because the fail-closed `--release` audit still rejects all packaging entry points.

| Exact command | Exit | Recorded result |
| --- | ---: | --- |
| `npm run verify` | `0` | PASS — Biome checked 498 files with no errors (108 non-blocking legacy warnings); TypeScript project references passed; main tests: 42 files / 380 tests passed; renderer tests: 60 files / 304 tests passed. |
| `npm run build` | `0` | PASS — main 131 modules, preload 3 modules, renderer 2,273 modules. Vite emitted one non-blocking notice that a HyperFrames module is both statically and dynamically imported. |
| `./node_modules/.bin/tsc -p tsconfig.remotion.json --noEmit` | `0` | PASS — standalone Remotion TypeScript project passed with no diagnostics. |
| `npm run verify:blocks` | `0` | PASS — rendered 23 blocks × 7 skins × the brand palette: all 161 combinations rendered; contact sheet written to `.gg/block-samples/brand/_grid.png`. The harness now serves `resources/` as its public directory, so bundled fonts loaded without the previous 404/fallback warnings. |
| `npm run audit:third-party` | `0` | Audit integrity PASS: 61 files verified; seven unresolved redistribution items reported as expected. |
| `npm run audit:third-party -- --release` | `1` | Expected fail-closed result: `Release refused: resolve every redistribution blocker before packaging.` |

### Release-gate repairs

- Applied the configured Biome formatting/import fixes and aligned lint severities with the existing release baseline; blocking lint/type/test failures were removed without changing product behavior.
- Kept TypeScript `strict` enabled while moving the standalone Remotion files back behind `tsconfig.remotion.json`; fixed missing imports/exports, stale public type barrels, caption preload typing, and release-contract tests.
- Corrected the re-hook override test to use the independent `enableRehook` setting and replaced two invalid redaction placeholders with inert test keys.
- Added `scripts/assert-clean-release-payload.mjs`; both `scripts/release-macos.sh` and `scripts/release-windows.sh` now list the packaged ASAR and fail before publishing when first-party payload paths contain environment/credential files, private keys, local caches, Python virtual environments, logs, editor scratch, agent scratch, or temporary/generated artifacts.

### Installer payload hygiene — PASS

The retained macOS DMG and Windows x64 unpacked NSIS payload were inspected with the same fail-closed scanner now wired into both release pipelines:

```bash
./node_modules/.bin/asar list \
  dist/release/macos-arm64/mac-arm64/BatchClip.app/Contents/Resources/app.asar \
  > /tmp/batchclip-mac-asar-list.txt
node scripts/assert-clean-release-payload.mjs \
  dist/release/macos-arm64/mac-arm64/BatchClip.app/Contents/Resources \
  /tmp/batchclip-mac-asar-list.txt

./node_modules/.bin/asar list dist/win-unpacked/resources/app.asar \
  > /tmp/batchclip-win-asar-list.txt
node scripts/assert-clean-release-payload.mjs \
  dist/win-unpacked/resources \
  /tmp/batchclip-win-asar-list.txt
```

Results:

- macOS retained payload: PASS, 26,455 ASAR paths plus first-party resources clean. The readonly mounted `BatchClip-0.1.0-mac-arm64.dmg` was scanned separately with the same result; SHA-256 `22fdb587035005ced6758cb1cdb520ea44641f7ea1b95b1018242a28a1804ebf`.
- Windows retained payload: PASS, 26,062 ASAR paths plus first-party resources clean; `dist/win-unpacked/BatchClip.exe` is PE32+ x86-64.
- Confirmed absent from both inspected payloads: secrets/credential files, `.env*`, private keys, local caches, Python venvs, `__pycache__`, logs, scratch/editor/agent files, and unrelated temporary/generated artifacts.
- Negative control: an ASAR fixture containing `/.env` made `node scripts/assert-clean-release-payload.mjs ...` exit `1` with `app.asar:/.env (environment file)`, proving the scanner fails closed.
- The release workspaces are created from an explicit source allowlist and use target-specific `npm ci`; host `node_modules`, `.ezcoder`, `.gg`, `dist`, `out`, and local Python environments are not copied into either installer. Python `extraResources` remains restricted to `*.py`, `requirements.txt`, and `pyproject.toml`.
- These retained artifacts are inspection evidence only and remain non-distributable. A fresh Windows NSIS installer and macOS DMG cannot be produced until the seven redistribution blockers are resolved and `npm run audit:third-party -- --release` exits zero.

## Unsigned macOS Apple Silicon DMG

This release is an internal, unsigned and unnotarized DMG for Apple Silicon Macs running macOS 11 or newer.
Intel (`x64`) Macs, universal binaries, ZIP archives, and Mac App Store packages are outside this release scope.

### Locked macOS release identity

| Field | Required value |
| --- | --- |
| npm package | `batchclip` |
| version | `0.1.0` |
| product | `BatchClip` |
| app ID | `com.batchclip.app` |
| target | DMG, macOS `arm64` only |
| minimum macOS | macOS 11.0 |
| app icon | approved `build/icon.icns` |
| signature | unsigned; no Developer ID certificate |
| notarization | disabled; no Apple notarization ticket |

`package.json` locks the DMG target to `arm64`, explicitly uses the approved ICNS for the app and DMG, sets `mac.identity` to `null`, sets `mac.notarize` and `dmg.sign` to `false`, and disables hardened-runtime and Gatekeeper-assessment expectations for this unsigned build.

### Reproducible macOS build command

Prerequisites:

- An Apple Silicon Mac running macOS 11 or newer.
- Node.js 22 or newer.
- npm access to the registries and binary downloads pinned by `package-lock.json`.
- Xcode Command Line Tools, including `codesign`, `lipo`, `hdiutil`, and `spctl`.
- A release snapshot that passes `npm run verify`.

From the repository root, run exactly:

```bash
npm run release:mac
```

`npm run build:mac` is an alias for the same isolated release pipeline.
The script copies the release inputs into a temporary workspace, installs locked optional/native dependencies for `darwin-arm64`, rebuilds `better-sqlite3` for Electron, builds the app, and invokes electron-builder with `--mac dmg --arm64 --publish never`.
Signing and notarization credentials are cleared before packaging, so credentials present in the caller's shell cannot silently change this release.

### macOS output artifact

For package version `0.1.0`, distribute only:

```text
dist/release/macos-arm64/BatchClip-0.1.0-mac-arm64.dmg
```

`dist/release/macos-arm64/mac-arm64/BatchClip.app` is retained for payload inspection and desktop deployment; it is not the release artifact.
Do not distribute an Intel, universal, ZIP, or older DMG from the `dist/` root.

### Automated macOS release assertions

The release command mounts the generated DMG and fails unless all of these checks pass:

- `BatchClip.app` and every checked native executable are thin `arm64` Mach-O binaries.
- `better_sqlite3.node` is rebuilt for the Electron ABI and passes an in-memory SQL smoke query.
- `ffmpeg-static` FFmpeg and `@ffprobe-installer` ffprobe are unpacked, executable, and `arm64`.
- Remotion source, `@remotion/bundler`, `@remotion/renderer`, the arm64 compositor/FFmpeg, Rspack binding, and esbuild are packaged at runtime-accessible paths.
- Python scripts, `requirements.txt`, and `pyproject.toml` are in `Contents/Resources/python`.
- Fonts, SFX, music resources, the Hyperframes catalog, and the runtime PNG icon are present.
- `Contents/Resources/icon.icns` exactly matches the approved `build/icon.icns`.
- Finder registers `.batchclip` as an editable `BatchClip Project` using `icon.icns`.
- Neither the app nor DMG has a Developer ID authority or Apple team identifier.
- Gatekeeper does not assess the unsigned app as trusted.
- No distributable macOS `x64` or universal app/artifact target is generated.

### Unsigned Gatekeeper limitation

This DMG is **not signed or notarized**, so Gatekeeper may block first launch with an unidentified-developer or unable-to-check-for-malicious-software warning.
This artifact cannot provide Apple-verified publisher identity, tamper assurance, or notarization malware scanning and must be shared only through a trusted internal channel with its SHA-256 checksum.
On a trusted test Mac, use Finder **Open** from the app's context menu or **System Settings → Privacy & Security → Open Anyway** when macOS offers it; do not describe this build as Gatekeeper-approved.
A public release requires a Developer ID Application signature and successful notarization before distribution.

### macOS manual acceptance

Record the DMG checksum:

```bash
shasum -a 256 dist/release/macos-arm64/BatchClip-0.1.0-mac-arm64.dmg
```

Test on a clean Apple Silicon account running macOS 11 or newer:

- [ ] The artifact is named `BatchClip-0.1.0-mac-arm64.dmg`.
- [ ] The DMG opens with the approved BatchClip icon and Applications shortcut.
- [ ] Gatekeeper shows the expected unsigned-app limitation; the documented trusted-build override allows launch.
- [ ] A `.batchclip` project displays the BatchClip association and opens in BatchClip.
- [ ] Python bootstrap can read the packaged scripts and requirements.
- [ ] A captioned vertical render succeeds at 1080×1920/30 fps using packaged FFmpeg, ffprobe, fonts, SQLite, and Remotion.
- [ ] A longform render succeeds at 1920×1080/30 fps.
- [ ] No artifact was signed, notarized, uploaded, or published.

### macOS end-to-end acceptance attempt — BLOCKED

**Recorded 18 July 2026 at source HEAD `0eb4697266eb0fe795d081eb69ae14b78ab9073d`.**

Final status: **BLOCKED / NOT RUN**. The available Mac meets the OS, architecture, disk, and network preflight, but it is not a clean account or VM. No app was copied to `/Applications`, no BatchClip process was launched, no existing app data or credential was removed, no acceptance checkbox above was changed, and this record is not release approval.

| Evidence | Result | Timing |
| --- | --- | ---: |
| Test host | BLOCKED — macOS 26.5.1 (build `25F80`) on Apple Silicon `arm64` is supported, but the active `groot` account contains 4.4 GB of existing BatchClip application data dating from 9 May 2026. The case variants `~/Library/Application Support/BatchClip` and `.../batchclip` resolve to that existing data on this case-insensitive volume. | Preflight only |
| Clean environment | BLOCKED — no clean account or VM is connected. `VBoxManage`, `utmctl`, `prlctl`, `qemu-system-aarch64`, `tart`, and `limactl` are absent; passwordless `sudo` is unavailable, so this run could not create and switch to a disposable macOS account without an interactive administrator action. | Preflight only |
| Required DMG | PASS for static artifact identity — `dist/release/macos-arm64/BatchClip-0.1.0-mac-arm64.dmg` exists, is 266,966,148 bytes, and has SHA-256 `22fdb587035005ced6758cb1cdb520ea44641f7ea1b95b1018242a28a1804ebf`. It mounted read-only as `/Volumes/BatchClip 0.1.0-arm64`. | `0.263 s` mount |
| Free space | PASS for preflight — 109 GiB was available, exceeding the requested 6 GB. Python/Parakeet installation was not started because the account was not clean. | `<1 s` |
| DMG layout and icons | PASS statically — the mounted root contains `BatchClip.app`, the `/Applications` shortcut, `.VolumeIcon.icns`, and `.background/dmg-background.png`. The approved `build/icon.icns`, packaged app icon, and volume icon all have SHA-256 `14e7b559deb5dd9fd1d3d85eef7ce49f2797baccc4917c9f37cf4d4b96a23b89`. Actual Finder and Dock rendering is NOT RUN. | Included in mount inspection |
| Application identity and `.batchclip` declaration | PASS statically — `CFBundleDisplayName=BatchClip`, `CFBundleIdentifier=com.batchclip.app`, version `0.1.0`, minimum macOS `11.0`, and a default Editor document type named `BatchClip Project` for extension `batchclip` using `icon.icns`. Finder registration, file icon display, double-click launch, and project handoff are NOT RUN. | Included in mount inspection |
| Architecture | PASS statically — `BatchClip.app/Contents/MacOS/BatchClip` is a thin Mach-O `arm64` executable. | Included in mount inspection |
| Gatekeeper provenance | BLOCKED — the retained local DMG has no `com.apple.quarantine` attribute, so it cannot reproduce a browser-delivered unknown-developer first launch as-is. No quarantine attribute was removed or bypassed during this attempt. | `<1 s` |
| Bundle signature assessment | FAIL — `codesign --verify --deep --strict --verbose=4` and `spctl --assess --type execute --verbose=4` both report `code has no resources but signature indicates they must be present`; `Contents/_CodeSignature/CodeResources` is absent. The main executable carries only a linker-generated ad-hoc signature (`TeamIdentifier=not set`). The expected warning and override must be retested after packaging is corrected. | `<1 s` |
| Redistribution release gate | FAIL (expected fail-closed) — `npm run audit:third-party -- --release` verified all 61 pinned files, reported the seven unresolved redistribution items, and refused release packaging. | `0.20 s` |
| Gemini key save/test | NOT RUN — no `GEMINI_API_KEY` or `GOOGLE_API_KEY` test credential is available to this shell. No credential was requested, read, logged, or recorded. | — |
| Representative source video | BLOCKED — the only repository candidates, `showreel-delos.mp4` and `landing/assets/showreel.mp4`, are the same 3,601,600-byte file (SHA-256 `96d9e9c14e43969adba77aa37b949eba293e23ac38629d37151cef19e1467310`). `ffprobe` reports 62 seconds of H.264 1080×1920/30 fps video and no audio stream, so it cannot exercise speech transcription or scoring. | `<1 s` probe |
| Python/Parakeet bootstrap, import, transcription, scoring, editing, and export | NOT RUN — requires the missing clean environment, a quarantined corrected build, a test Gemini credential, and representative spoken source media. No 1080×1920 export was produced or claimed playable. | — |
| Save/reopen project, logs, and uninstall | NOT RUN — requires a completed clean install and workflow. Existing account data and logs were deliberately not used as substitute evidence. | — |

#### Exact Gatekeeper override to test

These are the current macOS Tahoe 26 instructions from Apple Support, fetched 18 July 2026 from [“Open a Mac app from an unknown developer”](https://support.apple.com/guide/mac-help/open-a-mac-app-from-an-unknown-developer-mh40616/mac). They are **authoritative instructions, not execution evidence** for this blocked attempt:

1. Verify the DMG came through the trusted internal channel and its SHA-256 is exactly `22fdb587035005ced6758cb1cdb520ea44641f7ea1b95b1018242a28a1804ebf`, mount it, and drag `BatchClip.app` to the Applications shortcut.
2. In Finder, double-click `/Applications/BatchClip.app` once to trigger the unknown-developer warning.
3. Choose **Apple menu → System Settings**, click **Privacy & Security** in the sidebar, then go to **Security**.
4. Click **Open**, then click **Open Anyway**. Apple states that **Open Anyway** remains available for about one hour after the blocked launch attempt.
5. Enter the clean test account's login password, then click **OK**. The app is stored as an exception and subsequent launches can use the normal Finder or Dock action.

Do not substitute `xattr -d`, `spctl --master-disable`, or another command-line Gatekeeper bypass: those paths do not test the user-facing unknown-developer override requested for this release.

#### Recorded blockers and defects

- **REL-BLOCK-001:** the non-bypassable redistribution gate prevents producing a release-eligible DMG; seven legal/payload issues remain unresolved.
- **PKG-MAC-001:** the retained app bundle fails both strict code-signature verification and Gatekeeper assessment because signed Mach-O code has no bundle resource envelope. Rebuild the unsigned package so its intended ad-hoc/unsigned structure passes the project's packaging assertions before repeating the warning override.
- **QA-INFRA-002:** no clean supported Apple Silicon macOS account or VM is connected to this workspace.
- **QA-PROVENANCE-001:** the retained DMG lacks download quarantine provenance, so it cannot exercise the natural first-launch Gatekeeper warning without a fresh trusted-channel transfer.
- **QA-DATA-002:** no dedicated Gemini acceptance-test credential is available.
- **QA-DATA-003:** no representative spoken source-video fixture is available; both repository MP4 candidates are the same silent rendered showreel.

To resume: resolve all seven redistribution blockers and **PKG-MAC-001**, produce the exact unsigned DMG through `npm run release:mac`, transfer it through the trusted test channel so macOS applies quarantine, and run it from a clean Apple Silicon macOS 11+ account or VM with at least 6 GB free, network access, a dedicated Gemini test key, and representative spoken source media. Preserve timestamped screenshots of DMG/Finder/Dock/Gatekeeper/file association, setup and model-download timings, key test result with the secret redacted, source-media hash, transcript/score/edit evidence, exported-file hash plus `ffprobe` output, saved-project reopen evidence, the clean run's logs, and before/after uninstall filesystem evidence.

### macOS completion record

- Release snapshot HEAD: `________________________`
- macOS version: `________________________`
- Test machine CPU architecture: `arm64`
- DMG SHA-256: `________________________`
- Developer ID authority: `none`
- Notarization ticket: `none`
- Tester/date: `________________________`
- Final disposition: `INTERNAL UNSIGNED MACOS ARM64 RELEASE ACCEPTED / REJECTED`

## Unsigned Windows x64 installer

This release is an internal, unsigned installer for 64-bit Intel/AMD Windows 10 and Windows 11.
Windows arm64, Windows ia32, portable ZIPs, and per-machine installers are outside this release scope.

### Locked Windows release identity

| Field | Required value |
| --- | --- |
| npm package | `batchclip` |
| product | `BatchClip` |
| app ID | `com.batchclip.app` |
| target | NSIS, Windows `x64` only |
| signature | unsigned |
| install mode | per-user, no administrator rights |
| executable level | `asInvoker` |

`package.json` locks the NSIS target architecture to `x64`, sets `forceCodeSigning` to `false`, disables NSIS elevation, and disables the elevate helper. The release script also clears signing-certificate environment variables and sets `CSC_IDENTITY_AUTO_DISCOVERY=false` before packaging.

### Reproducible Windows release command

Prerequisites:

- Node.js 22 or newer.
- npm with access to the registries and binary downloads pinned by `package-lock.json`.
- A host supported by electron-builder for cross-building Windows NSIS packages; macOS and Linux hosts may require electron-builder's Wine tooling.
- A release snapshot that passes `npm run verify`.

From the repository root, run exactly:

```bash
npm run release:win
```

`npm run build:win` is an alias for the same verified release pipeline.

The script creates a temporary isolated source tree, installs optional and native dependencies from `package-lock.json` for `win32-x64`, downloads the matching `ffmpeg-static` binary, builds the app, and invokes electron-builder with all of these explicit constraints:

```text
--win nsis --x64 --publish never --config.forceCodeSigning=false
```

The temporary tree prevents Windows x64 native modules from replacing the development machine's modules. A successful run replaces only the generated `dist/release/windows-x64/` directory.

### Windows output artifact

For package version `0.1.0`, distribute only:

```text
dist/release/windows-x64/BatchClip-0.1.0-win-x64.exe
```

Updater metadata is generated beside it:

```text
dist/release/windows-x64/BatchClip-0.1.0-win-x64.exe.blockmap
dist/release/windows-x64/latest.yml
```

`dist/release/windows-x64/win-unpacked/` is retained for payload inspection and desktop test deployment. It is not the installer and must not be distributed as the release artifact.

Never distribute `dist/win-arm64-unpacked/`, a filename containing `arm64`, or any older artifact from the `dist/` root.

### Automated Windows release assertions

The release command fails before publishing its local output unless all of these checks pass:

- `BatchClip.exe` is a Windows x64 PE executable.
- `better_sqlite3.node` is a Windows x64 PE native module rebuilt for Electron.
- `ffmpeg.exe` and `ffprobe.exe` are Windows x64 PE executables.
- The NSIS installer has no Authenticode certificate table.
- `latest.yml` names `BatchClip-0.1.0-win-x64.exe`.
- No generated output path contains `arm64`.
- Python scripts and `requirements.txt` are packaged.
- Fonts, SFX, and music resource directories contain packaged files.
- The installer blockmap and updater metadata exist.

### Windows manual artifact verification

Record the final checksum without modifying the installer:

```bash
shasum -a 256 dist/release/windows-x64/BatchClip-0.1.0-win-x64.exe
```

On Windows, confirm the installer is unsigned:

```powershell
Get-AuthenticodeSignature .\BatchClip-0.1.0-win-x64.exe | Format-List Status,StatusMessage
```

Expected status: `NotSigned`.

### Windows 10/11 acceptance

Use a clean Windows 10 or Windows 11 x64 Intel/AMD test account without administrator privileges.

- [ ] The installer filename is `BatchClip-0.1.0-win-x64.exe`.
- [ ] Windows shows the expected unknown-publisher/SmartScreen warning for this internal unsigned build.
- [ ] Installation completes without a UAC administrator prompt.
- [ ] BatchClip installs for the current user and launches normally.
- [ ] `BatchClip.exe`, FFmpeg, ffprobe, and `better_sqlite3.node` are x64; no arm64 binary is present.
- [ ] Python bootstrap can read the packaged `python/*.py` files and `requirements.txt`.
- [ ] Bundled fonts load and a captioned render succeeds.
- [ ] Bundled SFX load; music resources are discoverable and missing optional tracks degrade gracefully.
- [ ] A local source video renders to playable 1080×1920 at 30 fps output with burned-in captions.
- [ ] A longform smoke render produces playable 1920×1080 at 30 fps output.
- [ ] Project save/open works and uninstall completes without elevation.
- [ ] No artifact was uploaded or published.

### Windows end-to-end acceptance attempt — BLOCKED

**Recorded 18 July 2026 at source HEAD `0eb4697266eb0fe795d081eb69ae14b78ab9073d`.**

Final status: **BLOCKED / NOT RUN**. No installer was launched, no acceptance checkbox above was changed, and this record is not release approval.

| Evidence | Result | Timing |
| --- | --- | ---: |
| Test host | BLOCKED — the available host is macOS Darwin `arm64`, not a clean Windows 10/11 x64 account or VM. `VBoxManage`, `utmctl`, `prlctl`, and `qemu-system-x86_64` are absent. | Preflight only |
| Required NSIS artifact | BLOCKED — `dist/release/windows-x64/BatchClip-0.1.0-win-x64.exe` is absent. Only the retained, non-distributable `dist/win-unpacked/` inspection payload exists. | Preflight only |
| Redistribution release gate | FAIL (expected fail-closed) — `npm run audit:third-party -- --release` exited `1`; audit integrity passed for 61 files, then packaging was refused for the seven unresolved redistribution items recorded at the top of this document. | `<1 s` |
| Windows free-space check | NOT RUN — no Windows test disk was available to verify the required 6 GB free. | — |
| Install, SmartScreen, icon, shortcuts, and `.batchclip` association | NOT RUN — requires the missing installer on a clean Windows x64 account. | — |
| Python/Parakeet 2–3 GB bootstrap | NOT RUN — requires the installed Windows application and network access. | — |
| Gemini key save/test | NOT RUN — requires the installed Windows application and a test Gemini credential. No credential was requested, read, or recorded. | — |
| Import, transcribe, score, edit, and 1080×1920 export | NOT RUN — requires the installed application, representative source media, completed Python setup, and Gemini connectivity. | — |
| Save/reopen project, inspect logs, and uninstall | NOT RUN — requires a completed Windows install and workflow. | — |

Recorded blockers/defects:

- **REL-BLOCK-001:** the non-bypassable redistribution gate prevents producing the required Windows NSIS installer; seven legal/payload issues remain unresolved.
- **QA-INFRA-001:** no clean Windows 10/11 x64 test account or VM is connected to this workspace.
- **QA-DATA-001:** no Windows-side test Gemini credential and representative source-video fixture are available to the acceptance run.

To resume: resolve all seven redistribution blockers, produce the exact unsigned NSIS artifact through `npm run release:win`, and run it from a clean non-admin Windows 10/11 x64 account with at least 6 GB free, network access, a test Gemini key, and representative source media. Preserve installer hash, screenshots, exported media/ffprobe output, `%APPDATA%/batchcontent/logs/`, setup/download timings, project reopen evidence, and the uninstall result.

### Windows completion record

- Release snapshot HEAD: `________________________`
- Windows edition/version: `________________________`
- Test machine CPU architecture: `x64`
- Installer SHA-256: `________________________`
- Authenticode status: `NotSigned`
- Non-admin install result: `PASS / FAIL`
- Tester/date: `________________________`
- Final disposition: `INTERNAL UNSIGNED WINDOWS X64 RELEASE ACCEPTED / REJECTED`
