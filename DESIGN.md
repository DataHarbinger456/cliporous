# BatchClip studio UI

## Design read

- **Surface:** Native desktop application UI for a repeated media-production workflow. BatchClip moves a creator from source ingestion through AI processing, clip approval, and rendering.
- **Audience:** A creator or editor working in a resizable Electron window, scanning many vertical clips and switching between quick approval decisions and slower AI/render progress.
- **Single job:** Make the next confident action obvious at every stage: bring in a source, understand the pipeline, approve the best clips, then export them.
- **Task and risk:** Ingestion and review are frequent, time-sensitive decisions. Processing and rendering are slower, failure-prone operations, so honest progress, recovery, and destination details must stay visible.
- **Content:** Source filenames, video thumbnails, clip scores, approval state, stage progress, render destinations, and truthful errors. Values can be long, missing, empty, or partially complete.
- **Platform:** Electron desktop window with mouse, keyboard, drag-and-drop, and resizable layouts. The production window now has a practical 900×640 minimum, with adaptive behavior verified at that split-screen size and a 320px reflow stress contract.
- **Constraints:** Light is the default and dark is an explicit persisted choice. Light uses a warm canvas with white raised surfaces; dark keeps the locked brand seeds: espresso `#23100c`, cream `#f6ecd9`, and violet `#9f75ff`. Existing shadcn/Radix primitives, Lucide icons, Inter font assets, routing, pipeline handlers, and render subscriptions remain the source of truth.

## Evidence and direction

The local application evidence favors stable geometry, compact controls, semantic status colors, and named transitions. The material model is neutral and classic in light mode, while the existing espresso palette remains available for low-light sessions. Linear and Superhuman are aligned references for stable application hierarchy, compact controls, and keyboard-friendly orientation. Intercom is a contrast reference for keeping status and recovery visible, but its heavier workspace chrome is not appropriate here.

## Design thesis

BatchClip is a **quietly cinematic clip studio**: a warm light canvas or deep espresso surface, readable foreground text, violet for action, and named accents for pipeline, media, AI, transcription, face detection, and rendering. The distinctive product device is a static filmstrip rule/grid behind the work surface, paired with an always-present **Source → Shape → Export** stage rail in the draggable header. It grounds the composition in the editorial sequence without competing with real media.

- **First glance:** wordmark, current stage, source status, and the one primary action.
- **Second glance:** current screen title, useful count or progress, and recovery/status details.
- **Primary actions:** choose/drop source, cancel/retry processing, approve/reject/open clip details, render/export.
- **Supporting evidence:** real thumbnails and video previews, source filename, score, stage progress, render destinations, and actual errors.
- **Geometry:** 8px rhythm; 12px cards; 8px controls; pill shapes only for compact statuses and approval chips; shared content edges use a `max-w-6xl` clip workspace and `max-w-3xl` focused stages.
- **Typography:** self-hosted Inter remains the stable UI family. Tighter display tracking and weight carry headings; monospace is reserved for paths, percentages, and token counts.
- **Color roles:** `background` is the warm light canvas or espresso dark surface; `card` is the raised work surface; `muted-foreground` is measured secondary copy; violet is action/focus; `success`, `warning`, `destructive`, `info`, and `signal` cover common states. Source indicators are named: orange for pipeline/media, purple for AI, blue for transcription, yellow for render, cyan for face detection, and red for errors. Each indicator also carries text, an icon, a badge, or native progress semantics.
- **Motion:** the existing 150ms screen transition remains the only spatial transition. Added feedback uses property-specific color, border, opacity, shadow, and progress transitions. Reduced motion removes non-essential movement while preserving immediate state changes.
- **Anti-default check:** no glass cards, ambient loops, fake metrics, emoji, oversized hero, equal feature-tile grid, decorative icon medallions, generic hover lift, or generated em dashes. The filmstrip layer belongs because it directly references video editing and remains static behind content.

## Primitive and component map

| Area | Reuse | Responsibility |
| --- | --- | --- |
| App shell | `App.tsx`, `ScreenFrame`, `ErrorLog`, `RecoveryPrompt`, `useDesktopLifecycle`, `window-state.ts`, `app-menu.ts` | Stage orientation, adaptive window chrome, route continuity, bounds/display persistence, native commands, recovery, lifecycle settlement |
| Controls | `Button`, `Input`, `Label`, `Select`, `Switch`, `Progress` | Native/Radix semantics and consistent geometry/focus |
| Surfaces | `Card`, `Alert`, `AlertDialog`, `Badge`, `Skeleton` | Work surfaces, status, recovery, confirmation, loading |
| Intake | `DropScreen`, `PythonSetupCard` | Source selection, URL/path submission, mode, recent projects, setup/error |
| Pipeline | `ProcessingScreen` | Honest stage timeline, source context, cancel/resume/retry/settings |
| Review | `ClipGrid`, `ClipCard`, `ClipDetail`, `TemplateEditor` | Media-first approval decisions and render entry points |
| Export | `RenderScreen` | Production queue, per-row progress/errors, retry/cancel/destination actions |
| Header status | `AiUsageIndicator`, `ThemeToggle`, and Lucide | Saved state, AI usage, theme, settings, current stage |

## Component states

- **Header:** current stage, source availability, saved/unsaved, AI usage, Light/Dark toggle, settings, narrow numbered rail.
- **Source intake:** idle, drag-over, browsing, URL/path entry, submitting, invalid source, Python setup required, setup error, recent-project rows, empty recent list.
- **Processing:** active stage, completed stage, pending stage, paused/resumable, error/retry, cancel confirmation, settings handoff, source filename.
- **Review:** loading, empty, populated, selected detail, approved, rejected, keyboard focus, missing thumbnail, render-all confirmation, render entry.
- **Export:** setup, queued, active, completed row, failed row, retrying, canceled, open folder, open CSV, completion footer.

Every status pairs color with text, icon, or native progress semantics. Async actions retain useful content and expose pending, success, failure, retry, and cancel behavior.

## Recovery continuity (PROJ-04)

- **Single job:** let a creator identify the exact autosave, understand what can return, and choose Restore or a separately confirmed Discard without risking the recovery file.
- **Snapshot identity:** every autosave receives a new id; legacy snapshots receive a stable content hash. Acknowledgement stores only that identity, so a later crash is always checked.
- **Project evidence:** the dialog shows project name, source name, localized autosave time, creator-facing stage, and counts for sources, transcripts, clips, and cut plans.
- **Safe restore:** parsing and state replacement finish before the snapshot is touched. A restored project is marked dirty and immediately receives a new atomic recovery-only snapshot without overwriting its project file; failed restores keep the prior file and remain retryable.
- **Destructive path:** Discard opens a second alert naming the project, autosave time, recoverable counts, and the exact boundary: source media and manually saved projects remain untouched. Failed deletion stays open and keeps the snapshot unacknowledged.
- **States and access:** load failure, restore pending/error, discard pending/error, success, and disabled duplicate submission are explicit. Radix supplies modal focus containment; visual, DOM, and keyboard action order match at narrow and desktop widths.
- **Thesis:** a calm edit-room recovery sheet uses the existing warm/espresso surfaces, violet primary action, Lucide icons, Inter type, and compact editorial metadata. No new visual system or decorative motion was introduced.

## Desktop lifecycle safety (PROJ-05)

- **Single job:** close, quit, and restart only after every project/settings save and processing/export cancellation has explicitly settled.
- **Native flow:** Electron owns the guard rather than an async `beforeunload`. Native dialogs name the project or active stage and keep the safest choice first.
- **Reference adaptation:** EZ Coder's main-owned workspace/relaunch continuity informs the coordination model; BatchClip replaces session language with project names, creator edits, processing stages, exports, and completed media.
- **Concrete choices:** unsaved work offers `Save and Quit/Restart`, `Quit/Restart Without Saving`, and `Cancel`; active work first offers `Keep Running in Background`, `Stop and Quit/Restart`, and `Cancel`.
- **Settlement:** the reusable `useDesktopLifecycle` hook reports live risk, runs renderer-owned saves/cancellation, and sends an explicit completion result. Main stays open on save failure, save cancellation, cancellation timeout, renderer loss, or stale active state.
- **Settings:** closing Settings separately offers `Save Changes`, `Discard Changes`, and `Cancel`. Its Advanced restart action uses the same project/job guard, and the shared restart bridge also covers update-initiated relaunches.
- **Continuity:** stopping processing preserves the source and completed/cached artifacts; stopping an export preserves completed outputs. Keeping work running minimizes the app rather than destroying its renderer.
- **Access:** platform-native message boxes provide keyboard traversal, Escape cancellation, system focus, and screen-reader semantics without introducing a second modal visual system.

## Exact workspace and missing media (PROJ-06 / PROJ-07)

- **Single job:** reopen a content project at the same editorial decision, then recover moved footage without sacrificing transcript, clip, brief, plan, or export evidence.
- **Continuity model:** schema v4 stores the active stage and source, selected clip, review filter and sort, inspector tab, grid scroll, per-clip playhead, Creative Brief, long-form plan, durable render rows, and completed output paths. The last saved project is restored behind a dedicated loading state so the drop screen never flashes first.
- **Reference adaptation:** EZ Coder's exact-session hydrate gate becomes a creator-workspace gate. Stable project artifacts load first; media availability is checked afterward because moved footage must not invalidate editorial decisions.
- **Offline media:** `OfflineMediaPlaceholder` is reused by review cards and the inspector. A Radix recovery dialog names every missing source and offers `Locate`, `Search folder`, `Remove`, and `Keep offline`; folder search rebases exact filename matches in one bounded scan.
- **Safety:** Locate refreshes real metadata while preserving source ids and all derived work. Remove uses a second confirmation naming the source and consequence. Preview, processing, and render controls are blocked only for the offline source; transcript, filters, inspector, brief, plan, and completed outputs remain readable.
- **Access and feedback:** actions use native buttons and Lucide icons, pending labels prevent duplicate submission, destructive copy distinguishes project removal from disk deletion, result counts announce filter changes, and the selected tab uses the existing Radix keyboard model.
- **Responsive result:** the relink dialog keeps one aligned card and both recovery paths visible at 1440×960 and the supported 900×800 practical minimum. Long paths truncate with a native title while the source name and every action remain visible.

## Editorial history and render trust (EDIT-01 / EDIT-02)

- **Single job:** let creators move fast through review and refinement while every reversible decision remains visible, reachable, and correctly scoped.
- **Reference adaptation:** EZ Coder's root-level, navigation-independent keyboard QoL becomes standard desktop editing commands. BatchClip routes history to the open clip inspector and otherwise to project-level review and bulk history.
- **Reusable control:** `HistoryControls` supplies the same action-specific labels, Lucide icons, disabled states, focus treatment, and shortcuts in the review toolbar and clip inspector.
- **Native contract:** the Edit menu mirrors the active history label and availability; its displayed accelerators defer to the renderer so text inputs keep native undo behavior.
- **Feedback:** every completed command announces a brief creator-facing result such as `Hook change undone`, `Approval restored`, or `Bulk rejection restored` through the existing accessible toast surface.
- **Render trust:** `Render All` passes explicit ids for one batch. Pending, approved, and rejected statuses remain unchanged, and the rejected-clip confirmation states that boundary before expensive work begins.

## Connections and AI cost trust (CONN-01 / USAGE-01 / USAGE-02)

- **Single job:** prove content-service readiness before a long analysis, then let creators audit what successful AI calls used and how the estimate was calculated.
- **Reference adaptation:** EZ Coder's provider hub and title usage meter become a creator-facing Connections panel plus one compact utility popover. Provider internals stay out of the creative workspace.
- **Reusable connection anatomy:** `ConnectionCard` keeps provider name, encrypted key field, test action, explicit status, recovery feedback, and feature impact aligned across Gemini, Pexels, and fal.ai.
- **Complete states:** encrypted-key hydration shows `Checking configuration`, empty services show `Not configured`, entered keys show `Configured, not tested`, checks show `Testing`, and outcomes show `Connected`, `Connected, degraded`, `Invalid`, `Test failed`, or `Optional and unavailable`. Editing a tested value resets the claim immediately.
- **Graceful degradation:** Gemini names the AI editing features that stop while local transcription, manual review, and rendering remain safe. Pexels names stock B-roll as the only loss. fal.ai is honestly unavailable in this build and does not solicit a new key.
- **Usage bridge:** one root hook subscribes the preload event and updates session totals, capped call history, per-feature aggregates, and per-model aggregates in Zustand. Cleanup removes the listener on renderer teardown.
- **Auditable estimates:** the shared ledger lists exact model ids, paid-tier input/output rates, source URL, version `google-gemini-api-2026-07-17`, and checked date. Provider-reported thinking tokens are included because Gemini bills them as output.
- **Unknown and free-tier truth:** unlisted model calls are excluded rather than assigned another model's rate; partial/unavailable estimates are labeled. The popover says free-tier calls may cost zero and the provider bill is final.
- **Access and adaptation:** native inputs/buttons and Radix menu behavior preserve labels, keyboard focus, pending/duplicate-action protection, async status announcements, narrow card stacking, and reduced-motion progress feedback.

## Informed local-tool setup (SETUP-01)

- **Single job:** let a creator understand and deliberately start the one-time local speech-model install, then recover without reselecting footage or risking project files.
- **Reference adaptation:** EZ Coder's calm prerequisite behavior becomes a creator-facing setup surface. It states the missing capability, keeps technical detail subordinate, uses one explicit action, and preserves the selected video while work completes.
- **Informed action:** the preflight names the 2–3 GB download, typical 10–30 minute duration, exact app-managed storage path, 6 GB free-space requirement, measured free space, required connection, and the boundary between offline local tools and internet-dependent Gemini/YouTube features. Readiness checks never start a download.
- **Complete flow:** checking, ready-to-start, offline, low disk, installing by stage, overall progress, cancelling, canceled, failed, retry, ready, incomplete/repair, and Settings repair use the shared `PythonSetupCard` and one root `usePythonSetup` controller per renderer window.
- **Continuity:** a source chosen during the readiness race is added to the project, named in the setup card, and starts automatically only after setup succeeds. Cancel and failure retain the source and completed download artifacts.
- **Local model contract:** setup now downloads `nvidia/parakeet-tdt-0.6b-v3` into the app-managed model cache and stamps the requirements plus model contract only after verification. Runtime transcription points to the same cache, preventing the first transcription from triggering a surprise model download.
- **Repair and safety:** Advanced Settings exposes `Repair content tools`; retry is idempotent, setup cancellation terminates tracked downloads and child processes, and media IPC handlers fail with the setup/repair action instead of installing invisibly.
- **Responsive and access:** native buttons and progress semantics keep focus/status behavior predictable; Lucide icons pair with text; the action stays visible at 390×844 after the narrow revision; 900px and desktop layouts preserve the same content order without decorative motion.

## Adaptive desktop windows and native menu (WIN-01 / WIN-02)

- **Single job:** keep source intake, progress, clip decisions, and export controls usable in a split-screen production window while standard desktop commands stay reachable from the OS menu.
- **Native window contract:** the main window defaults to 1180×760 and can shrink to 900×640; Settings can shrink to 400×480. The shared `window-state.ts` utility restores normal bounds, maximized state, and display id, constrains stale/off-screen bounds to the current work area, and persists moves/resizes without recording minimized or full-screen geometry.
- **Responsive recomposition:** below 1024px the app header becomes a two-row brand/actions plus stage rail; processing keeps a compact two-column status/work layout from 820px; review and render headers use explicit grid rows before their controls would wrap unpredictably; the clip grid reaches two useful columns at 760px. Every stage keeps its primary action visible and its content region scrollable at 900×640.
- **Platform chrome:** macOS keeps the hidden-inset title bar and traffic-light clearance; Windows keeps native titlebar controls and a visible menu bar. The settings window uses native chrome on both platforms and remains explicitly resizable/maximizable.
- **Native menu:** File exposes New Project, Open, dynamic Open Recent, Save, and Save As. Edit mirrors active Undo/Redo labels. View exposes platform zoom/full-screen roles. Settings, Keyboard Shortcuts, What’s New, Check for Updates, and About move to platform-standard menu locations and accelerators. New Project protects dirty or active work, while signed updates check quietly and surface progress only when a release is available or the creator asks.
- **Reference adaptation:** EZ Coder's small-window resilience and dense desktop shortcuts were adapted to creator tasks, while BatchClip keeps its editorial stage rail, media-first review, and calm cut-room visual language.

## Creator project lobby (HOME-01 through HOME-05)

- **Single job:** start a creator project from local footage or a YouTube URL, or resume a saved cut with enough media and stage evidence to choose confidently.
- **Reference adaptation:** EZ Coder's `HomeScreen` contributes a strong first action and quiet prerequisite handling; `ProjectPicker` contributes compact search, recent-item scanning, explicit empty/no-match states, and separate new/open flows. BatchClip replaces coding atmosphere, sessions, and source badges with saved poster frames, selected frames, source filenames, output shape, clip/select counts, editorial stage, media availability, and last-opened time.
- **Evidence:** local application UI remains the primary system. Linear and Superhuman are the two aligned application references for stable action placement and dense scan paths; Intercom remains the contrast reference because its heavier workspace framing would compete with footage here.
- **Thesis:** the lobby is the front bench of the Creator Cut Room. A static contact sheet of real project frames appears before the intake surface; the drop/choose action is primary, YouTube is an explicit alternative, and project search follows in the same aligned content column. Neutral geometric frames appear only when no safe saved media exists.
- **Reusable components:** `ProjectContactSheet` owns real-frame composition and neutral fallback; `RecentProjectLibrary` owns loading, error/retry, first-use empty, no-results, search, recent/pinned sorting, stage/media metadata, and project actions; `NewProjectDialog` collects only name, source, and output mode before analysis.
- **Project actions:** Open, Reveal, Pin, Rename, Duplicate, Remove from Recents, and separately confirmed Delete Project File use the typed preload bridge. Rename and duplicate atomically rewrite project identity and paths; delete names the file boundary and leaves source videos and exports intact.
- **Media metadata:** recent-project records retain one validated embedded image poster plus up to three approved/select frames, the source name, output mode, stage, counts, pin state, and missing-project/media status. Unsafe or oversized image values are rejected before entering recent metadata.
- **Native project files:** electron-builder registers `.batchclip`; the Windows NSIS installer is machine-wide because the installed builder only supports NSIS file associations in per-machine mode. Startup argv, macOS `open-file`, and second-instance launches route into one focused window. Startup opens take priority over automatic last-project resume, while runtime opens protect dirty or actively processing work before replacing the workspace.
- **Access and motion:** source drag has a Choose Video button alternative; every form field has a persistent label; search result counts and failures use status semantics; row actions use Radix keyboard/focus behavior; destructive deletion has a separate confirmation; all feedback uses named color/border/shadow transitions and resting frames remain still.
- **Responsive result:** at 1440px the contact sheet, split intake surface, and compact project row form one editorial scan. At the supported 900px minimum all primary actions, source options, search, and project metadata remain visible. At 390px the DOM order stacks predictably, keeps Drop Video visible, and reduces the contact sheet to three still frames without horizontal overflow.
- **Anti-default revision:** the first render exposed an oversized recent row, so the poster was constrained to a fixed 112px editorial strip. No extra dashboard tiles, metrics, ambient motion, external GIFs, glass, or generic hover lift were added.

### HOME release evidence

- Focused tests pass: 7 main project-handler tests, 2 native project-file integration tests, 11 IPC registry tests, and 6 DropScreen tests, including contact-sheet metadata, rename/duplicate/delete, startup/second-instance routing, source drop, URL intake, queued setup, missing connection, named New Project with optional brief, recent rendering, and search.
- The Electron/Vite production build passes. Targeted Biome checks pass for all HOME implementation files.
- The full main run passed 362/362 before the final two focused project-file tests were added; those two pass in the final targeted run. The full renderer run passes 195/196; the unrelated pre-existing `project-service · desktop save semantics` assertion expects one `clearRecovery` call after a sequence that currently performs two.
- Repository typecheck remains blocked by the documented broad strict/project-boundary baseline; filtered output contains no direct HOME implementation type error.
- Captures: `.ezcoder/screenshots/home-lobby-desktop-revised.png` (1440×900), `.ezcoder/screenshots/home-lobby-900.png` (supported minimum), `.ezcoder/screenshots/home-lobby-390.png` (reflow stress), and `.ezcoder/screenshots/home-new-project-final.png` (disabled initial submit plus optional brief disclosure). The deterministic harness uses frames extracted from the checked-in `showreel-delos.mp4`, not downloaded stock or invented people.
- Rendered rubric: **22/24**. Specificity 2; hierarchy 2; composition 2; consistency 2; typography 2; surface logic 2; state completeness 2; responsive 2; accessibility 1 (source/tests pass, native assistive-tech audit unverified); motion 2; authenticity 2; distinctiveness 1 (restrained cut-room shell with media-led signature).
- Native file-association installation, Windows Explorer double-click, macOS Finder double-click, screen-reader traversal, 200% OS text scaling, and forced-colors remain manually unverified in packaged builds.

## Creator-studio shell (SHELL-01 through SHELL-06)

- **Single job:** keep project identity, save truth, stage orientation, and the next creator command reachable from Source through Shape and Export without replacing the existing flow.
- **Reference adaptation:** EZ Coder's `ZoomController`, keyboard-first shell, compact utility grouping, and searchable command behavior were adapted to a creator cut room. Terminal language, developer controls, and Matrix styling were deliberately excluded.
- **Header truth:** `StudioHeader` and `ProjectIdentityStatus` show the project name, active source, explicit `Saving`, `Saved`, `Unsaved changes`, `Save failed`, or new-project state, plus the ordered Source, Shape, Export stage list. The same project, dirty, and running context updates the native window title through `document.title`.
- **Command system:** `Cmd/Ctrl+K` opens `CommandPalette`, a searchable, arrow-key navigable Radix dialog. It covers project files, valid stage jumps, selected-clip decisions, selected/approved renders, Creative Brief, Template, Settings, output, logs, theme, sound preference, zoom, and shortcuts. Disabled actions remain focusable and explain the exact missing source, clip, approval, media, or idle-job prerequisite. Creator Profile remains visibly unavailable until the separate profile system exists, rather than implying a fake profile was configured.
- **Project and app scope:** filter, sort, selected inspector, inspector tab, grid scroll, and preview playhead remain in the versioned project workspace. Grid density, inspector width, activity-feed expansion, completion-sound preference, and zoom use the app-scoped `display-preferences` service. Theme remains in its existing cross-window persisted service.
- **Display controls:** the consistent More menu exposes comfortable/compact grids, three inspector widths, activity feed, sound, theme, zoom, Settings, and shortcuts. `ClipGrid`, `ClipDetail`, and `ErrorLog` consume the shared preferences instead of duplicating state.
- **Zoom:** CSS UI zoom persists from 50% to 200% in 5% steps. `Cmd/Ctrl +`, `-`, and `0`, the View menu, the header menu, and command palette all reach the same source of truth. Storage plus BroadcastChannel synchronize open windows, and `ZoomController` shows a finite percentage HUD. At 150% and above, the header hides the low-priority wordmark and action labels before geometry can overlap; the command palette becomes an inset full-window surface with a fixed search row and scrollable results.
- **Platform truth:** the preload exposes the actual Electron platform. Shared copy uses `Reveal in Finder`, `Show in Explorer`, or `Show in File Manager`; logs use matching native terminology; shortcut labels use Command glyphs on macOS and Ctrl text on Windows/Linux. Windows redo remains `Ctrl+Y`.
- **Adaptive composition:** the stage rail is now an ordered progress list rather than non-interactive navigation. Save, Commands, and More keep a stable order on every screen. Lower-frequency Open, Save As, display, theme, settings, and help actions collapse into More before familiar primary controls lose labels.
- **Access and motion:** the palette keeps native buttons inside a Radix dialog, restores focus on close, supports search plus Arrow/Home/End navigation, and keeps disabled reasons available to keyboard users with `aria-disabled`. Search focus uses one inset ring without clipping; the percentage HUD is announced politely and follows the existing reduced-motion override.
- **Anti-default revision:** the first palette capture exposed a clipped double focus ring and an overlapping close control, so search focus moved to one inset wrapper ring and reserved close-button space. The first 200% capture exposed header overlap and an off-screen palette search field, so high zoom now compacts header identity and recomposes the palette rather than shrinking text or hiding commands. No dashboard tiles, glass, ambient motion, emoji UI, generic hover lift, or extra visual motif was added.

### SHELL release evidence

- Targeted checks pass: 15 main tests and 30 renderer tests cover the application menu and persisted zoom handoff, IPC registry, keyboard palette shortcut, command search and disabled reasons, zoom HUD/persistence, platform labels, project identity, clip grid, and render labels.
- The complete main suite passes **366/366**. The complete renderer suite passes **205/206**; the one failure is the documented pre-existing project-service assertion expecting one `clearRecovery` call after a sequence that performs two.
- The Electron/Vite production build passes. Targeted Biome checks pass for all SHELL implementation files.
- Repository typecheck remains blocked by the documented broad strict/project-boundary baseline. Filtered node and web TypeScript output contains no direct SHELL implementation error.
- Captures: `.ezcoder/screenshots/shell-project-identity-desktop.png`, `shell-command-palette-project.png`, `shell-zoom-200-final.png`, and `shell-palette-zoom-200-final.png`.
- Rendered rubric: **22/24**. Specificity 2; hierarchy 2; composition 2; consistency and flow 2; typography 2; material logic 2; state completeness 2; responsive behavior 2; accessibility 1 (source, focus, keyboard, and zoom pass; native assistive-technology and forced-colors audit unverified); motion 2; authenticity 2; distinctiveness 1 (intentionally restrained creator cut-room shell).
- Native Windows titlebar pixels, packaged macOS/Windows menu dispatch, screen-reader traversal, forced colors, and OS-level text scaling remain manually unverified. CSS UI zoom is verified at the supported 900×640 minimum through 200% with no overlap and with command search/actions reachable.

## Transparent background processing (PROCESS-01 through PROCESS-08)

- **Single job:** let a creator verify that real media is moving through transcript, moment, story, face, timing, and styling stages while completed evidence remains useful after navigation, cancellation, failure, or restart.
- **Reference adaptation:** EZ Coder's `ActivityBar` contributes stable elapsed/status geometry and explicit cancellation; `LiveToolPanel` contributes a rolling three-row feed, collapse/detail behavior, and settled results. BatchClip replaces code, tools, tokens, and agents with creator language for transcripts, candidate moments, stitched stories, speaker framing, timing, styling, and exports.
- **Real source:** `SourceMediaSummary` shows the saved poster frame, duration, source dimensions, locked output dimensions, a compact real filmstrip, and an FFmpeg-derived audio waveform. Missing poster/waveform states remain neutral and truthful.
- **Activity and proof:** `ProcessingActivityFeed` shows the latest three meaningful creator operations by default and expands to retained detail. Completed stages settle into real transcript word counts, candidate score ranges, stitched counts, timing removed, speaker-aware coverage, long-form plan counts, and style-decision counts. Unknown language and unavailable fallback counts are omitted rather than invented.
- **Honest timing:** every active stage keeps elapsed and ETA geometry reserved. ETA remains `Estimating…` until three monotonic samples span at least eight seconds and five percent; stale or non-linear samples never produce an estimate. Known pauses receive stage-specific explanations after 12 seconds.
- **Durable model:** `CreatorJob` records project/source identity, stage, progress, timestamps, cached source path, completed stages, activity, proof, failure state, and outputs outside the mounted processing screen. App-scoped jobs persist locally; the current safe checkpoint is also embedded in project/recovery JSON. Interrupted active work reopens explicitly paused with its artifacts, failed stage, and resume explanation.
- **Background control:** `JobsHud` is mounted in `StudioHeader` on every stage and the lobby. It reports running, queued/paused, completed, and failed counts with compact progress rows plus Open, Stop, Retry, Reveal, and Dismiss actions. `Keep working in background` returns to the project lobby without unmounting or cancelling the active chain.
- **Native feedback:** the main process mirrors work through `BrowserWindow.setProgressBar`; native notifications cover ready clips, completed exports, and action-required failures. Clicking a notification restores/focuses the window and routes to its durable job. The existing notification preference is now visible in Settings.
- **Preservation:** the primary action is `Stop and keep progress`. Source, transcript, clips, cached media, completed proof, and stage checkpoint remain. `Start over and discard cache` sits behind a named destructive confirmation and leaves the source video on disk.
- **Reusable components:** `SourceMediaSummary`, `ProcessingActivityFeed`, `JobsHud`, `CreatorJob`, job progress/ETA helpers, and root `useNativeJobIntegration` separate media evidence, activity behavior, global control, durability, and native effects.
- **Access and motion:** native buttons, lists, progress semantics, status announcements, Lucide icons, visible labels, stable geometry, named color/border/opacity transitions, and the existing reduced-motion rule remain the contract. No tool names, emoji UI, ambient motion, hover lift, glass treatment, fake metrics, or generated em dashes were added.
- **Responsive behavior:** the processing card uses media/evidence plus stage columns from 820px and stacks in DOM order below it. Both columns live in the existing scroll region; the narrow source column repeats the same `Work in background` and `Stop and keep progress` actions before the activity feed, so the primary controls remain visible at 390×844.

### PROCESS release evidence

- Focused verification passes: **47 renderer tests** cover source evidence, stop preservation, minimum-window composition, Jobs HUD behavior, native progress/notifications, ETA stability, checkpoint restoration, pipeline routing, and Settings; **12 main tests** cover IPC registry completeness.
- The complete main suite passes **366/366**. The complete renderer suite passes **214/215**; the one failure is the documented pre-existing project-service assertion expecting one `clearRecovery` call after a sequence that performs two.
- The Electron/Vite production build passes. Targeted Biome checks pass for all new job, processing, activity, media, ETA, native-integration, and checkpoint files.
- Repository typecheck remains blocked by the documented broad strict/project-boundary baseline. Filtered web and node output contains no direct PROCESS implementation error.
- Captures: `.ezcoder/screenshots/process-transparent-desktop-final.png`, `.ezcoder/screenshots/process-transparent-narrow-final.png`, and `.ezcoder/screenshots/process-jobs-hud.png`. The poster is an extracted frame from the checked-in `showreel-delos.mp4`; the harness labels the source's real 62-second duration and 1080×1920 dimensions and reports its missing audio waveform honestly.
- Rendered rubric after one revision: **22/24**. Specificity 2; hierarchy 2; composition 1 (deliberately quiet space remains after the compact live stage list); consistency and flow 2; typography 2; material logic 2; state completeness 2; responsive behavior 2; accessibility 1 (source semantics, targets, status, and narrow actions pass; packaged screen-reader/forced-colors audit unverified); motion 2; content authenticity 2; visual distinctiveness 2.
- Packaged macOS/Windows notification permission behavior, notification-click routing across a closed non-current project, native dock/taskbar pixels, screen-reader traversal, 200% OS text scaling, and forced colors remain manually unverified.

## Fast clip review (REVIEW-01 through REVIEW-06)

- **Single job:** let one creator sort many candidate moments, make reversible decisions at keyboard speed, and retain media/edit context while moving between the contact sheet and inspector.
- **Reference adaptation:** EZ Coder's root-level shortcuts, modal focus ownership, compact action feedback, and exact workspace continuity become clip review controls. Coding language and terminal styling remain excluded.
- **Filters and order:** one typed `buildReviewItems` path owns All, Unreviewed, Approved, Rejected, and Stitched filtering plus deterministic Score, Source Time, Duration, and Status ordering. Filtered result counts announce separately from total clips, and no results remains distinct from an empty project.
- **Keyboard contract:** Left/Right and J/K move in visible order; Space controls only the active inspector player; A, X, and U set review status; E/Enter opens the editor; R opens an explicit selected/approved render confirmation; Cmd/Ctrl+Z and redo target review history. Text fields, sliders, nested dialogs, and render confirmations retain ownership of their keys.
- **Continuity and feedback:** decisions use the existing global history, show one replace-in-place accessible toast with Undo, and restore the decided clip when Undo is clicked. Hook and trim commits use the same reusable feedback path with clip-scoped history. Auto-advance defaults on, persists as an app display preference, moves to the next visible unreviewed clip, preserves scroll/focus, and can be turned off from the review key strip.
- **Master-detail:** at 1280px and wider the contact sheet remains visible beside a persistent `ClipDetail` panel sized by the existing inspector-width preference. Below 1280px the same inspector content remains in the current Radix Sheet. Filter, sort, selected clip, tab, playhead, scroll, and local editor state survive responsive recomposition.
- **Inspector navigation:** previous/next buttons and the same Arrow/J/K path retain current filtered order, expose disabled boundaries, and announce `N of total`. A selected badge separates keyboard position from approval status without adding another decorative surface.
- **Access and motion:** native controls, Radix focus containment on the narrow Sheet, `aria-keyshortcuts`, visible focus, result/status live regions, textual status labels, and named border/opacity transitions carry the interaction. No surprise audio, ambient motion, hover lift, emoji UI, or extra confirmation for reversible decisions was added.
- **Responsive revision:** the first capture gave selected and approved cards similar outlines and compressed a long Sheet title. The revision added a functional Selected badge, removed the duplicate selected shadow, moved narrow inspector navigation below the full title, and kept all three decision actions on one compact footer row.

### REVIEW release evidence

- Focused coverage now includes **24 cases** across filter/sort helpers, persisted auto-advance, keyboard triage, text/composite/dialog ownership, action-scoped undo, explicit keyboard render, narrow Sheet behavior, wide master-detail behavior, playhead/in-flight edit recomposition, hook-edit undo, and existing ClipDetail behavior. Final execution is deferred to the commit gate.
- Latest pre-review evidence: the Electron/Vite production build passed, targeted REVIEW Biome checks passed, and the full renderer suite passed 229/230 with only the documented pre-existing `clearRecovery` assertion. The final ideal-review undo/menu-ownership adjustments await the commit gate; repository-wide check/typecheck retain their documented baseline failures.
- Captures: `.ezcoder/screenshots/review-master-detail-desktop-final.png`, `.ezcoder/screenshots/review-master-detail-1280.png`, `.ezcoder/screenshots/review-sheet-narrow-final.png`, and `.ezcoder/screenshots/review-auto-advance-undo-final.png`. The deterministic harness uses a frame extracted from the checked-in `showreel-delos.mp4` and labels its fixture project locally.
- Rendered rubric after one revision: **22/24**. Specificity 2; hierarchy 2; composition 2; consistency and flow 2; typography 2; material logic 2; state completeness 2; responsive behavior 2; accessibility 1 (keyboard/focus/status source and tests pass; packaged screen-reader and forced-colors audit unverified); motion 2; content authenticity 2; visual distinctiveness 1 (intentionally restrained media-led cut room).
- Packaged screen-reader traversal, forced-colors, 200% OS text scaling, RTL stress, real decoder performance with a large clip set, and macOS/Windows focus-ring pixels remain manually unverified.

## Editorial clip detail (REVIEW-07 through REVIEW-11)

- **Single job:** let a creator hear, trim, and judge one candidate against the output treatment without waiting blindly or treating a model score as a result forecast.
- **Reference adaptation:** EZ Coder's rolling live-tool and activity feedback becomes a quiet media-review status line. It names `queued`, `rendering`, `ready`, cached, and failed states while the editable live layout guide remains in place. Developer orchestration, terminal language, and provider internals stay out of the cut room.
- **Waveform trim:** `WaveformTrim` calls the existing FFmpeg waveform bridge once per source/duration and caches normalized samples. The waveform sits behind a selected range with two independently named Radix handles, source-relative speech-gap and suggested-cut markers, numeric timecode inputs, and `Reset to Auto`, so drag is never the only path.
- **Rendered preview:** `useRenderedPreview` sends the current local trim, crop, words, hook, caption mode, accent, hook-title settings, and auto-zoom settings through the existing low-quality render-preview API after a 500ms debounce. Exact clip/settings cache hits display immediately. New edits invalidate in-flight results; stale output paths are discarded and cleaned, replacement paths evict prior versions, and clip replacement, Sheet close, or inspector unmount clean owned cache entries. Failure keeps every text/trim edit available with Retry.
- **Playback:** `EditorialPlayer` replaces opaque native chrome with explicit play/pause, a selection playhead, five-second seeks, 30fps frame nudges, Shift plus comma/period 100ms nudges, replay, loop, mute, and persisted volume. Playback never autostarts; audio defaults muted and can only begin after an explicit click or Space action. Hover-card previews remain unchanged and muted.
- **Decision aid:** `Director's note` labels the overall number and optional loop/original values as AI estimates, preserves saved model rationale as `Model rationale`, names rescoring changes, and explicitly says the score is not a prediction of audience performance. The creator is asked to judge opening, context, pacing, and audience fit.
- **Responsive composition:** at 700px and wider the reusable player puts a 150px vertical picture beside compact transport controls, keeping rendered-preview truth and the score disclaimer above the fixed decision footer at both 1440×900 master-detail and 900×760 Sheet sizes. Below 700px it stacks with a 220px picture and the same DOM/focus order.
- **Access and trust:** native inputs/buttons, named range thumbs, textual waveform summary, visible status text, `aria-live` async feedback, `aria-keyshortcuts`, non-color marker legends, and persistent error recovery cover the interaction. The render status says `Preview quality` and notes that final export can contain additional effects rather than claiming pixel-final output.

### REVIEW-07 through REVIEW-11 release evidence

- Commit-time `/commit` diagnostics must re-run targeted formatting, typing, tests, and the Electron/Vite production build after the final ideal-review adjustments. No compiler-clean claim is made here.
- Source coverage includes 11 focused preview/ClipDetail cases plus the existing ClipGrid keyboard-ownership case, covering waveform API use, keyboard trim handles, Reset to Auto, non-blocking preview generation, playback/audio controls, uncertainty-framed rationale, exact cache hits, debounce, stale-result cleanup, failure, Retry, and advanced editorial shortcuts.
- The last pre-review full renderer run completed 236/237 before the final ideal-review adjustments; its only failure was the existing project-service assertion expecting one `clearRecovery` call after a sequence that performs two. The commit gate must establish the final result.
- Repository typecheck has a documented broad strict/project-boundary baseline. Final changed-file diagnostics remain unverified until `/commit`.
- Pre-final captures: `.ezcoder/screenshots/review-editorial-desktop-final.png` at 1440×900, `.ezcoder/screenshots/review-editorial-narrow-final.png` at 900×760, and `.ezcoder/screenshots/review-editorial-workflow-tall.png` for the full score rationale. They use the checked-in `showreel-delos.mp4` and labeled fixture bridge responses; final recapture remains pending after the ideal-review adjustments.
- **Revision gate:** the first 1440×900 capture hid transport controls below the inspector fold. The revision recomposed media and controls side by side, reduced the panel picture to 150px, moved the uncertainty note ahead of trim, and surfaced `89/100 AI estimate` beside its heading. The pre-final desktop and narrow captures keep playback, preview truth, score uncertainty, and review decisions visible together.
- Provisional rendered rubric: **22/24**. Specificity 2; hierarchy 2; composition 2; consistency and flow 2; typography 2; material logic 2; state completeness 2; responsive behavior 2; accessibility 1 (semantic source and keyboard paths are present; final diagnostics, packaged screen-reader, and forced-colors audits are unverified); motion 2; content authenticity 2; visual distinctiveness 1 (intentionally restrained and media-led).
- Real encoder latency, final automated diagnostics, packaged screen-reader traversal, forced colors, 200% OS text scaling, RTL stress, Windows codec playback, and macOS/Windows focus-ring pixels remain unverified.

## Durable export payoff (EXPORT-01 through EXPORT-09 and EXPORT-13)

- **Single job:** turn approved regular clips, stitched stories, and accepted long-form edits into durable finished media without hiding queue work, local constraints, visual substitutions, or recoverable failures.
- **Reference adaptation:** EZ Coder's background-task continuity, compact activity rows, elapsed truth, and global recovery actions become a creator production queue. Engine names, terminal treatment, and developer controls remain outside the primary surface; the payoff is playable exported media.
- **Unified queue:** `RenderProgress` is the shared item contract for regular, stitched, and long-form work. It persists kind, label, source, duration, order, options hash, estimate, output path, preparation history, visual fallbacks, and safe checkpoints in project/recovery state. Reopened active rows return queued, completed files are verified before being trusted, and only missing or unfinished media returns to the resumable queue.
- **Preflight trust:** `ExportPreflight` checks the real destination, free disk, source availability, selected encoder, hardware acceleration, locked resolution/fps, quality, item count, and total media duration. Only unavailable destination, insufficient working space, or missing source media block; missing optional B-roll/image connections state the exact playable degradation.
- **Honest estimates:** output size uses media duration, output profile, quality/CRF, and bounded bitrate ranges. Render time uses duration, preset, encoder class, and up to eight matching recent local timings. Early values remain labeled estimates with a wider range; measured history narrows the range without claiming certainty.
- **Queue control:** queued rows have button and keyboard-operable earlier/later alternatives, per-item cancel, one-item retry, retry all failed, clear completed, and reveal actions. `Stop after current` lets active encodes finish and keeps remaining jobs; immediate cancellation remains a separate explicit action.
- **Preparation and fallbacks:** render preparation events are translated into filler cleanup, captions, B-roll, visual cards, crop, overlays, and encode language, with only the current and most recent activity visible. Segment fallback events stay attached to the affected row with concrete speaker-shot outcomes plus Fix and Render again actions.
- **Media payoff:** `CompletedOutputCard` renders a real local video contact sheet with explicit Play/Pause, platform reveal, Copy path, and Render again. Failed rows stay in the production queue beside successful evidence rather than disappearing into a generic success state.
- **Native behavior:** active local processing or rendering owns an Electron power-save blocker and releases it when no running job remains. Dock/taskbar progress continues through the global jobs model. Completion and attention notifications are sent only while the app is unfocused, and a click restores the app at the durable export job.
- **Access and motion:** native buttons, progress semantics, polite status copy, textual status labels, Lucide icons, visible focus, no-drag reorder alternatives, named property transitions, and the existing reduced-motion contract remain the floor. No surprise audio, emoji UI, ambient motion, hover lift, fake metrics, or decorative export dashboard was added.

## Restrained delight and release polish (DELIGHT-01 through DELIGHT-06)

- **Voice system:** lead with the creator artifact and outcome: `Building your selects`, `Your selects are ready`, `Export pack ready`, and `Needs attention`. Use source, moment, select, cut, frame, caption, and export language in primary UI. Put provider, encoder, process, and raw stage names behind Details. Errors state what happened, what remains safe, and one next action. Copy stays direct, truthful, count-specific when real data exists, and avoids generated em dashes.
- **Sound:** `ui-sounds.ts` maps approve, reject, job-ready, clean-batch, warning, and failure events to three quiet UI-only assets adapted from EZ Coder. The app preference defaults off, appears in Settings and the header utility menu, and playback is best-effort. Hover, autosave, launch, and exported content never trigger these files.
- **Celebration:** the root `CompletionCelebration` renders one finite 1.25-second accent burst for the first successful export and each later zero-failure batch. The first-export claim persists locally. Reduced motion renders no burst, failed/partial batches never celebrate, and resting screens remain still.
- **Updates and release notes:** `electron-updater` checks GitHub Releases quietly on launch and hourly, downloads only after consent, reports real progress, and installs only after active work stops and dirty project/queue state saves. NSIS and macOS update verification use the platform code signatures; packaging fails closed through `forceCodeSigning`. What’s New opens once when a prior installed version changes, never on first install, and remains available from Help.
- **Branded native surfaces:** one static clapperboard/playhead mark now feeds the app icon, BrowserWindow, About panel, DMG, assisted NSIS installer, and release dialogs. The first-launch lobby continues to use real footage and a static contact sheet. Crash copy names safe saved work before technical details. No terminal, code, Matrix, particle ambience, or external decorative imagery appears.
- **Skeleton geometry:** `CreatorSkeletons` reuses the local Skeleton primitive without pulse motion and reserves exact project-poster/list/action-rail, 9:16 inspector, and export-row geometry. Launch and recent-project loading use these shapes; real rows crossfade over the same dimensions without moving the current target.
- **Reuse map:** `BrandMark`, `ReleaseSurfaces`, `CompletionCelebration`, `ProjectListSkeleton`, `ClipInspectorSkeleton`, and `ExportQueueSkeleton` are reusable across the lobby, shell, review, export, Settings, and future release windows.
- **Anti-default decision:** the single brand-mark enclosure belongs because it is product identity in native and renderer chrome, not a repeated section decoration. All other surfaces stay flat, aligned, media-led, and still.

### DELIGHT release evidence

- Signed packaging requires real Apple/Windows signing identities and GitHub release credentials. Unsigned local installer output is intentionally blocked; packaged signature verification and update installation remain unverified until those external certificates are present.
- UI sounds remain opt-in, are never inserted into media, and swallow autoplay/decode failures. Reduced-motion celebration behavior is source-enforced.
- **Automated proof:** targeted Biome passes for all DELIGHT implementation files; the Electron/Vite production build passes; all 290 renderer tests pass; 33 focused updater/menu/sound/celebration/processing/native-job/command/IPC tests pass. The full main run reaches 371/374, with three unrelated pre-existing failures in long-form prompt fixtures and Rehook override behavior. Repository-wide check and typecheck retain the broad dirty-tree baseline (670 Biome errors and 1,193 TypeScript errors), while filtered TypeScript output contains zero direct DELIGHT implementation errors.
- **Packaging proof:** `electron-builder --dir --config.forceCodeSigning=false --publish never` validates the macOS package, icon, resources, and complete release configuration. Production config restores `forceCodeSigning: true`; a real signed artifact and update install remain externally unverified because this machine has no Apple Developer ID or Windows signing identity.
- **Rendered proof:** `.ezcoder/screenshots/delight-update-desktop.png`, `delight-update-narrow.png`, `delight-whats-new-desktop.png`, `delight-whats-new-narrow.png`, `delight-skeleton-desktop.png`, and `delight-skeleton-narrow.png` cover 1440×900 and 390×844. Banner actions remain visible, the dialog keeps focus/action order, and skeleton geometry stays static without pulse motion.
- **Critique revision:** the first DMG artwork duplicated destination frames behind native app icons, so those decorative boxes and labels were removed. What’s New intentionally omits the reference app’s confetti so first-export and clean-batch remain the only celebratory moments.
- **Rendered rubric:** **23/24**. Specificity 2; hierarchy 2; composition 2; consistency and flow 2; typography 2; material logic 2; state completeness 2; responsive behavior 2; accessibility 1 (semantics, focus, labels, reduced motion, and 390px reflow pass; packaged screen-reader and forced-colors remain unverified); motion 2; content authenticity 2; visual distinctiveness 2.
- **Explicitly unverified:** real update-server latency, signed macOS/Windows installation, screen-reader traversal, forced colors, 200% OS text scaling, and field performance with a large media library require packaged cross-platform hardware. The production renderer bundle is measured at 2.62 MB JavaScript before transfer compression; no field INP or media-decoder measurement is claimed.

## Responsive behavior

- **1280px and wider:** one-row app chrome and full stage rail; review becomes a contact-sheet plus persistent inspector, while export retains its one-row toolbar.
- **900px to 1099px:** app chrome recomposes into brand/actions plus a full-width stage rail; review/export actions occupy a deliberate second row; processing keeps source/progress context beside the live stage list.
- **320px stress case:** controls reflow in DOM order, essential text remains readable without fixed content heights, long filenames/hook text wraps, clip cards preserve usable media geometry, and the primary action stays visible or one obvious action away.
- **All sizes:** preserve existing screen routing, keyboard order, drag alternative, labels, `aria-pressed` and role contracts, 44px practical targets, no-hover usability, and reduced-motion behavior.

## Accessibility and release contract

- Use native buttons, inputs, selects, labels, lists, progress semantics, and existing Radix dialogs.
- Keep visible focus rings, logical DOM/action order, accessible icon-only labels, and asynchronous status text.
- Verify keyboard traversal and completion, focus return from dialogs, 200% text zoom, 320px reflow, long filenames, missing media, empty/loading/error/retry/disabled/success/destructive states, reduced motion, and forced-colors where Electron exposes it.
- Do not rely on color, position, motion, or icon alone. Preserve drag-and-drop alternatives and truthful error recovery.

## Required release evidence checklist

- [x] Representative desktop Light and Dark renderer states verified through the bridge harness at 1440x900.
- [x] All four production stages verified at the real 900×640 minimum; Settings verified at 400×480.
- [x] Native menu template, platform accelerators, dynamic recent-project payloads, window-bound fitting, and IPC registry have focused regression tests.
- [x] Production build and full suites passed (359 main / 192 renderer) before the final four test-only regressions were added; those tests were source-reviewed and intentionally not rerun per review instruction.
- [ ] Repository-wide `npm run check` passes. The existing baseline currently reports 698 errors outside the targeted WIN files.
- [ ] Repository-wide `npm run typecheck` passes. Existing strict/project-boundary failures remain; filtered output has no new direct WIN implementation error.
- [x] PROJ-06/07 project round-trip, exact last-project resume, media detection/search, Locate/Search/Remove UI, and IPC contract tests pass.
- [ ] Native Windows titlebar pixels, screen-reader traversal, 200% OS text scaling, and forced colors remain unverified manually.
- [x] Renderer source scan finds no `transition-all` or raw palette status classes in `src/renderer/src`. UI states pair color with text, icons, badges, or native progress semantics.
- [x] Light/Dark persistence, pre-paint initialization, and cross-window convergence are covered by focused renderer tests.
- [x] Light and dark desktop plus narrow captures are re-verified after the theme change.

## Evidence log

- WIN-01/02 verification: the production build and all 359 main / 192 renderer tests passed before the final test-only review edits. Focused source coverage now includes 6 window-state tests, 3 platform-menu tests, 5 keyboard-shortcut tests, 4 Settings tests, the processing minimum-size regression, and 11 IPC registry tests; the four final regressions were source-reviewed and intentionally not rerun per review instruction.
- WIN-01 minimum-size captures: `.ezcoder/screenshots/win-01-drop-900x640.png`, `win-01-processing-900x640.png`, `win-01-clips-900x640.png`, and `win-01-render-900x640.png`. Settings resizing is captured at `.ezcoder/screenshots/win-01-settings-400x480.png`; desktop review is captured at `win-01-clips-1440x900.png`.
- WIN-01 revision gate: the deliberate two-row app header and grid-based review/export toolbars kept every primary action visible at 900×640; processing retained source context, stage progress, and Cancel simultaneously; Settings retained tab access, content scrolling, and fixed Save at 400×480. No decorative element was added.
- WIN-01/02 rendered rubric: **22/24**. Specificity 2; hierarchy 2; composition 2; consistency 2; typography 2; surface logic 2; state completeness 2; responsive 2; accessibility 1 (source/tests pass, native assistive-tech audit unverified); motion 2; authenticity 2; distinctiveness 1 (restrained cut-room adaptation).
- Desktop captures at 1440x900: `.ezcoder/screenshots/theme-light-desktop.png`, `.ezcoder/screenshots/theme-dark-desktop.png`, and `.ezcoder/screenshots/theme-settings-advanced.png`.
- Narrow captures at 390x844: `.ezcoder/screenshots/theme-light-narrow-verified.png` and `.ezcoder/screenshots/theme-dark-narrow.png`.
- Capture method: production renderer build served locally with the deterministic preload bridge harness, which allowed real screen composition and theme toggling without secrets or media files. Native Electron window pixels remain unverified in this headless environment.
- Current PROJ-06/07 verification: 151/151 renderer tests and 337/337 main tests passed; the production Electron/Vite build passed. Exact workspace round-trip and relink actions have focused regression coverage.
- PROJ-06/07 deterministic captures: `/tmp/cliporous-missing-media-desktop.png` (1440×960), `/tmp/cliporous-missing-media-narrow.png` (900×800), and `/tmp/cliporous-restored-workspace.png` (selected clip plus restored Transcript tab). Native Electron media-dialog pixels remain unverified headlessly.
- Revision gate: kept the static filmstrip/grid layer because it is specific to video editing, preserved the 8px geometry and 150ms screen transition, centralized source indicators, and verified both material modes plus the Advanced Appearance control.
- Manual checks still needed in a real Electron window: stage rail at 1280px and 900px, source drag and browse focus flow, processing error and resume, clip keyboard approval, render retry/cancel, 320px reflow, 200% zoom, reduced motion, and forced colors.
- Recovery tests cover per-snapshot identity, legacy hashing, project details, failed restore/discard preservation, recovery-only refresh, date normalization, and normal-project metadata stripping; final execution is pending the commit-time gate.
- Recovery layout captures: `.ezcoder/screenshots/recovery-desktop.png` (1440×900), `.ezcoder/screenshots/recovery-narrow.png` (390×844), and `.ezcoder/screenshots/recovery-discard-confirm.png` (900×700). The final discard copy additionally lists source and cut-plan counts; recapture is pending the commit-time gate.
- Recovery revision gate: the first capture exposed reversed narrow visual/focus order from the shared footer; the component now overrides that layout so DOM, keyboard, and visual order match. The final narrow capture confirms the correction.
- Recovery manual checks still unverified in the native Electron window: screen-reader announcement, forced-colors rendering, 200% OS text scaling, and filesystem failure behavior on macOS/Windows.
- Pre-review PROJ-05 evidence recorded 335 main tests, 146 renderer tests, 3 focused lifecycle settlement tests, 9 IPC registry tests, and a production build. Commit-time verification remains pending after the final pipeline cancellation test was added.
- Repository-wide check and typecheck have known baseline failures recorded above. No compiler-clean claim is made for the changed files until commit-time diagnostics run.
- Native close/quit/restart dialog pixels and cross-platform title-bar behavior remain unverified in this headless environment; action labels, safe defaults, and cancellation ids were source-reviewed, while wait-for-settlement behavior is covered by focused bridge tests.
- EDIT-01/02 verification: 337/337 main tests and 159/159 renderer tests pass; the production Electron/Vite build and targeted Biome check for all changed files pass.
- EDIT-01/02 captures: `.ezcoder/screenshots/edit-history-desktop.png` (1440×900), `.ezcoder/screenshots/edit-history-narrow.png` (900×800), `.ezcoder/screenshots/edit-history-feedback.png` (action-specific toast plus redo state), and `.ezcoder/screenshots/edit-history-inspector.png` (clip-scoped controls).
- Repository-wide `npm run check` and `npm run typecheck` remain blocked by pre-existing formatting and strict-TypeScript failures outside this change; no full-gate pass is claimed.
- CONN-01 / USAGE-01 / USAGE-02 verification: all 349 main tests and 180 renderer tests pass, including the IPC registry, provider health outcomes, billed thinking-token capture, bridge-to-store/header regression, exact/unknown model pricing, and connection-state UI. The Electron/Vite production build passes.
- Targeted Biome checks pass for every new usage/connection module and touched renderer file. Repository-wide `npm run check` still reports 712 pre-existing diagnostics, and `npm run typecheck` still reports broad pre-existing strict/project-boundary failures; no full-gate pass is claimed.
- Connection captures: `.ezcoder/screenshots/connections-desktop.png` (900×900 configured state), `.ezcoder/screenshots/connections-tested-degraded-final.png` (connected plus degraded), `.ezcoder/screenshots/connections-narrow-revised.png` (390×844), and `.ezcoder/screenshots/connections-320.png` (320×800 reflow). `.ezcoder/screenshots/usage-ledger-final.png` shows the real header utility popover with a partial estimate and unlisted model.
- Revision gate: the first 390px capture cramped provider descriptions beside status badges, so the card header now recomposes vertically below 640px. The first usage capture exposed low-contrast open-trigger text, so the active/hover foreground now uses `accent-foreground`. Decorative provider medallions were omitted; one small Lucide glyph is enough.
- Final rendered rubric: **22/24**. Specificity 2 (content services and degraded creator features); hierarchy 2 (provider, status, action, impact); composition 2 (shared card anatomy); consistency 2 (local primitives/tokens); typography 2 (Inter plus mono ledger); surface logic 2 (cards and utility popover have distinct jobs); state completeness 2 (loading through failed/unavailable); responsive 2 (900, 390, and 320 captures); accessibility 1 (labels/statuses/targets verified in source and tests, native keyboard/forced-colors audit unverified); motion 2 (property-specific, reduced-motion-safe feedback); authenticity 2 (real provider ids/rates and labeled fixtures); distinctiveness 1 (faithful cut-room system, intentionally restrained settings surface).
- Production checks: async duplicate prevention, error recovery, key preservation, exact feature impact, unknown-model exclusion, free-tier caveat, pricing source/version/date, responsive reflow, and listener cleanup pass. Native Electron keyboard traversal, 200% OS text scaling, forced colors, and screen-reader announcements remain explicitly unverified; no blocking defect was observed in automated semantics or rendered captures.
- SETUP-01 verification: all 351 main and 191 renderer tests pass, including 6 setup-card tests, 5 DropScreen tests with queued-source continuation, 3 Settings tests, and 11 IPC registry tests. Targeted Biome checks and the Electron/Vite production build pass. Repository-wide `npm run check` remains blocked by pre-existing diagnostics, and typecheck remains blocked by the existing strict/project-boundary baseline; filtered logs report no new direct SETUP-01 implementation type errors.
- SETUP-01 captures: `.ezcoder/screenshots/setup-first-run-desktop.png`, `.ezcoder/screenshots/setup-first-run-900-revised.png`, `.ezcoder/screenshots/setup-first-run-390-revised.png`, `.ezcoder/screenshots/setup-progress-desktop.png`, and `.ezcoder/screenshots/setup-offline-900.png`. Fixtures use a clearly local placeholder path and measured disk value; native installer/network pixels remain unverified.
- SETUP-01 revision gate: the first 390px capture placed the primary action below the viewport, so setup copy and narrow spacing were tightened without removing any requirement. The revised 390×844 capture keeps the informed facts and `Download local tools` action visible together.
- SETUP-01 rendered rubric: **22/24**. Specificity 2; hierarchy 2; composition 2; consistency 2; typography 2; surface logic 2; state completeness 2; responsive 2; accessibility 1 (source/tests pass, native screen reader and forced colors unverified); motion 2; authenticity 2; distinctiveness 1 (restrained creator setup, intentionally not decorative).

## Creative Brief, Creator Profile, and creator-focused Settings (BRIEF-01 / PROFILE-01 / PROFILE-02 / SETTINGS-01)

- **Single job:** keep project guidance, reusable creator identity, brand assets, and app defaults visible at the moment a creator needs to make or audit a decision.
- **Reference adaptation:** EZ Coder's Notes modal contributes direct autosaved drafting; its Memory modal contributes inspectable source, scope, updated date, edit, delete, empty, and destructive states; its Settings behavior contributes compact categories and explicit credential saves. BatchClip replaces agent memory and developer concepts with audience, tone, CTA, references, platform, safe zones, long-form defaults, output, studio, storage, and creator diagnostics.
- **Brief trust boundary:** Creative Brief draft fields autosave with the `.batchclip` project, while a separate committed snapshot is the only version added to scoring and stitching prompts. The panel labels both facts and exposes a disabled initial Save action until guidance exists.
- **Reusable profile model:** app-scoped Creator Profiles store audience, tone, CTA text/URL/assets, logo, evidence/capture assets, reference links/files, target platform, safe-zone positions, long-form skin, and palette. Asset imports are copied into stable app storage and rechecked through a typed IPC health request; missing files remain named and revealable.
- **Project override model:** every project stores the selected profile id and explicit override keys. Each override is visibly labeled and has `Use profile default`; `Clear all overrides` removes only project values. Selecting a profile applies platform, safe-zone, audience, and long-form defaults without changing the locked vertical PRESTYJ, caption-mode, or violet-accent rules.
- **Transparent memory:** remembered rows are derived only from explicitly saved profile fields. Every row shows value, source profile, reusable scope, updated date, and Edit/Delete actions. Delete clears the actual saved field and metadata rather than hiding an audit row. Project edits never silently become permanent preferences.
- **Settings information architecture:** the dedicated window now uses Connections, Output, Studio, Storage, and Advanced. Credentials retain Test plus Save; non-secret output, appearance, sound, notifications, zoom, promo, cleanup, and developer preferences autosave. Storage shows measured temp/cache/log sizes, manual cleanup, startup cleanup, autosave interval, and logs without invented metrics.
- **Reusable pieces:** `CreatorAssetField`, `SafeZoneFields`, `ProfileField`, the Creator Profile persistence service, committed-brief prompt builder, existing Radix dialogs/tabs/selects, shared display preferences, and the central IPC registry avoid parallel UI or persistence systems.
- **Responsive and access:** desktop uses a profile list plus editor; narrow windows stack the same DOM order and keep the three profile tabs, name, delete, and first guidance fields visible. Tables retain semantic headers and horizontal containment. Radix owns focus/Escape/return behavior; native controls, persistent labels, status text, Lucide icons, and named transitions preserve the existing application contract.
- **Anti-default revision:** no new dashboard tiles, glass, gradients, ambient motion, emoji, decorative medallions, or hover lift were introduced. The only compact storage value blocks report real local measurements. After capture, the empty Creative Brief Save action was disabled until content exists; the profile asset-health effect was also narrowed to asset changes instead of every keystroke.

### BRIEF / PROFILE / SETTINGS release evidence

- Focused verification passes: 13 main IPC-registry tests and 43 renderer tests covering project round-trip/migration, prompt commit boundaries, reusable profile persistence, transparent memory deletion, profile modal behavior, display persistence, command access, New Project profile/brief submission, settings reorganization, provider health, and adaptive navigation.
- The Electron/Vite production build passes. Targeted Biome checks pass for implementation files. Repository-wide typecheck remains blocked by the documented broad strict/project-boundary baseline; filtered output has no direct renderer/profile implementation errors.
- The full suites remain blocked by unrelated current-branch failures: main passes 370/373 with two `[REDACTED]` test literals and one rehook expectation failing; renderer passes 252/253 with the existing Cut Plan render-spy expectation failing.
- Captures: `.ezcoder/screenshots/creative-brief-desktop.png`, `creator-profile-desktop.png`, `creator-profile-narrow.png`, `creator-memory-desktop.png`, `creator-settings-desktop.png`, and `creator-settings-narrow.png`. The deterministic browser bridge uses clearly local illustrative profile values and no customer claims.
- Rendered rubric after one revision: **22/24**. Specificity 2; hierarchy 2; composition 2; consistency and flow 2; typography 2; material logic 2; state completeness 2; responsive behavior 2; accessibility 1 (source, semantics, focus primitives, and narrow layout pass; packaged screen-reader/forced-colors audit unverified); motion 2; authenticity 2; visual distinctiveness 1 (intentionally restrained creator cut-room system).
- Packaged macOS/Windows asset pickers, filesystem permission failures, 200% OS text scaling, forced colors, screen-reader traversal, and startup cleanup timing remain manually unverified.

## Real-content profile and long-form treatment preview (PROFILE-04)

- **Design read:** this is an editorial application surface for creators making a medium-cost, reusable brand decision. Its single job is to compare one long-form treatment against the open project's frame and words before saving a Creator Profile default or spending render time.
- **Reference adaptation:** EZ Coder's picker contributes stable section headers, compact repeatable choices, explicit empty/error recovery, and actions kept beside their scope. Its Settings modal contributes persistent labels, preserved form values, clear save/cancel order, and concrete failure copy. BatchClip keeps the warmer paper-like canvas, serif treatment inside rendered-skin previews, and media-first hierarchy.
- **Thesis and reuse map:** the real project preview is the first decision surface; seven container-responsive skin thumbnails and exact palette colors follow as evidence. `LongformStylePreview`, `SkinThumbnail`, `PalettePicker`, `PaletteEditor`, `PaletteDeleteDialog`, and the shared contrast utility keep Creator Profiles, Cut Plan review, project setup, and re-render setup on one CRUD and preview contract.
- **Data and defaults:** custom palettes remain app-scoped and become valid Creator Profile defaults. Selecting a treatment with a profile applied creates or clears explicit project overrides; `Use profile defaults` restores both axes. Deleting a shared palette repairs selected project settings, project overrides, and every reusable profile reference to Brand Default.
- **Validation and states:** names are required, limited to 40 characters, and unique across built-in and custom palettes. Colors accept normalized six-digit hex, invalid fields retain input and receive linked errors, and WCAG text/graphic ratios produce non-blocking warnings. First-use custom empty, missing saved palette, source checking, source offline, speaker-only plan, destructive confirmation, disabled render, and successful save states are distinct.
- **Re-render setup:** accepted-plan status, source frame, phrase/block/card counts, speaker-only fallback, source relink recovery, no-AI-reanalysis assurance, Creator Profile defaults, and the next export treatment now share one card before preflight.
- **Responsive and access:** native fieldsets/buttons/inputs provide semantics; Radix owns dialog and confirmation focus behavior; selected state pairs ring plus check text; icon actions have visible labels and 44px minimum height. Container units keep thumbnail typography stable from the 390px two-column picker through the wide desktop preview. No hover lift, ambient motion, emoji UI, or `transition-all` was added.

### PROFILE-04 release evidence

- Targeted Biome checks pass for 13 implementation/test files. The production Electron/Vite build passes.
- Focused renderer verification passes **9/9** tests for contrast math, real-content preview, all seven skins, creation validation, custom-palette selection/deletion, missing-palette repair, profile-reference repair, and visual Creator Profile defaults. Existing RenderScreen tests pass **9/9**.
- The full renderer run passes **273/276**. The three failures are the documented current-branch baselines: two project-settings fixture expectations and the Cut Plan render-spy expectation. Repository typecheck remains blocked by existing project-wide strict errors; filtered output reports no direct PROFILE-04 implementation errors.
- Captures: `.ezcoder/screenshots/profile04-palette-desktop-final.png`, `.ezcoder/screenshots/profile04-palette-narrow-final.png`, and `.ezcoder/screenshots/profile04-editor-final.png`. The harness is explicitly labeled as a local preview fixture and uses an existing local project frame rather than invented customer proof.
- **Revision gate:** the first narrow capture used overlay-only edit/delete icons and repeated profile-default metadata. The revision moves CRUD into visible 44px text actions and removes the redundant skin-level default label. The large preview remains the only surface that repeats real project copy, preventing card-grid noise and duplicate accessible content.
- Final rendered rubric: **22/24**. Specificity 2; hierarchy 2; composition 2; consistency and flow 2; typography 2; material logic 2; state completeness 2; responsive behavior 2; accessibility 1 (semantics, labels, focus primitives, measured contrast rules, and target sizes pass; packaged screen-reader/forced-colors traversal remains unverified); motion 2; authenticity 2; visual distinctiveness 1 (the seven honest render-skin structures carry the signature, while the control chrome intentionally stays restrained).
- Packaged Electron keyboard traversal, 200% OS text scaling, forced colors, screen-reader announcements, RTL/localization stress, image-decode performance on very large posters, and native relink pixels remain explicitly unverified.

## Transcript-led bulk review (REVIEW-15 through REVIEW-18)

- **Single job:** move from a remembered phrase to the right source moment, create or compare candidates, and finish repeated review decisions without losing the keyboard cursor or current clip context.
- **Reference adaptation:** EZ Coder's project picker contributes inline search, explicit no-match/count states, compact result rows, and stable header wrapping. Its selection QoL contributes visible counts, Select All/Clear/Done controls, direct keyboard entry, and actions that name their scope. BatchClip keeps media, source time, transcript evidence, scores, and review states as the visible decision material.
- **Transcript navigation:** `TranscriptNavigator` searches exact contiguous words or phrases against word timestamps, keeps the selected clip's saved transcript and source range beside the player, moves the paused source player to any result, supports explicit Start/End range marking, and creates an honestly unscored candidate with timed words. Empty transcript, no match, offline media, disabled creation, selected range, and successful creation remain distinct.
- **Bulk selection:** checkbox mode is separate from the single keyboard review cursor. Native checkboxes, S/Space toggling, Shift plus Arrow range extension, Cmd/Ctrl+A, Escape, visible Select All/Clear/Done actions, and counted Approve/Reject/Unreviewed/Render/settings actions keep mouse and keyboard paths equivalent. One global history snapshot makes status and shared-setting changes cheap to undo across regular and stitched candidates.
- **Comparison:** exactly two standard candidates open `ClipComparisonDialog`. Both source players seek and play at one relative time, the shorter duration defines the shared window, audio defaults muted and can be assigned to A or B, and score, rationale, duration, source time, hook, and status remain aligned beneath each picture. Hook Polish continues to own hook-variant text comparison.
- **Density:** Comfortable and Compact remain app-scoped display preferences and are now directly reachable from review. Both modes retain score plus absolute source time, hook context, and 32px action targets; Compact increases contact-sheet columns and collapses only repeated button labels.
- **Responsive and access:** source identity, result counts, keyboard guide, and selected count remain outside modal content. The review toolbar wraps at the supported 900px minimum; transcript navigation becomes source/detail columns from 760px and otherwise stacks in DOM order; compare stays side by side where useful and stacks below that. Radix provides dialog focus/Escape/return behavior, native search/range/checkbox controls provide keyboard semantics, and playback never starts or unmutes without explicit action.
- **Anti-default check:** no new visual motif, dashboard metrics, glass, ambient motion, emoji, hover lift, or generated em dash was added. The only new badges and borders encode source time, current clip range, checked state, or review status.

### REVIEW-15 through REVIEW-18 release evidence

- The pre-final focused run passed **32/32** tests across ClipGrid, ClipDetail, transcript matching/ranges, candidate creation, multi-select keyboard extension, regular-plus-stitched bulk decisions, counted bulk status/render, one-step undo, synchronized comparison controls, persisted density, media URL handling, and honest manual-score treatment. The pre-final production build passed. Final native-card semantics and discontinuous stitched-range adjustments await `/commit` diagnostics; no compiler-clean claim is made here.
- The latest full renderer run passed **264/267** before the final adjustments; the subsequently added stitched-bulk case passed in its focused run. The three failures were current-branch baselines: two project-settings fixture expectations and the Cut Plan render spy. Repository typecheck remained blocked by the documented broad project baseline.
- Captures: `.ezcoder/screenshots/review-15-18-selection-desktop-final.png`, `review-15-18-selection-narrow-final.png`, `review-15-transcript-desktop-final.png`, `review-15-transcript-narrow-final.png`, and `review-17-compare-desktop-final.png`. The harness uses the checked-in showreel and an explicitly local transcript fixture. The final semantic revision does not change the selection/compare composition; transcript-range recapture is deferred to `/commit`.
- **Revision gate:** the first 1440px render compressed `Review your clips` into a three-line sliver because the expanded toolbar competed in the same grid row. The revision gives project identity and truthful counts their own row, then lets search, density, filters, history, template, and render actions wrap below. Desktop and the supported 900px minimum preserve source context, every selected-count action, and useful contact-sheet area.
- Pre-final rendered rubric: **22/24**. Specificity 2; hierarchy 2; composition 2; consistency and flow 2; typography 2; material logic 2; state completeness 2; responsive behavior 2; accessibility 1 (native semantics, keyboard routes, focus primitives, count/status announcements, and target sizes are present; packaged screen-reader and forced-colors remain unverified); motion 2; content authenticity 2; visual distinctiveness 1 (intentionally restrained, media-led review workspace).
- Packaged screen-reader traversal, forced colors, 200% OS text scaling, RTL/localization stress, Windows local-video seeking, and performance with a multi-hour transcript remain manually unverified.

## Deterministic UX release gate (QA-01 through QA-12)

- **Design read:** this is release evidence for a desktop creator cut room. Its single job is to make every consequential state directly reproducible, keyboard-operable, privacy-safe, and visually inspectable at the 900px production minimum without replacing production components with a demo shell.
- **Showcase and authenticity:** `#qa` exposes 19 fixed states through the real App, Settings, processing, review, Cut Plan, queue, recovery, connection, media, and error components. The neutral poster says `LOCAL QA FIXTURE`, paths use `/QA-Fixtures`, timestamps are fixed, and the fixture clock is frozen. No customer claims, stock people, provider credentials, or personal paths enter evidence.
- **Interaction contract:** native Chromium page zoom replaces CSS-only scaling so media queries reflow from 50% to 200%. At high zoom, Jobs and More stay visible while Save and Commands move into the same More menu. Template positioning adds numeric X/Y, buttons, Arrow, Shift+Arrow, Reset, and live snap announcements beside drag. Command palette closure now returns focus to the invoking control.
- **Performance contract:** below-first-row posters are lazy; media dimensions remain reserved; clip preview videos mount only after hover/focus; completed outputs use `preload="none"`; one completed output can play at a time; media cards use `content-visibility`. The full renderer run measured a 120-card jsdom mount at 188.8ms with zero initial video decoders.
- **Responsive and state result:** desktop master-detail, 900px main minimum, 400px Settings minimum, effective 450px viewport under native 200% zoom, light/dark, reduced motion, high contrast, cancellation, partial success, full success, recovery, and macOS/Windows reveal copy are captured. The first critique found clipped high-zoom utilities and cramped output actions; the revision constrained the shell to the viewport and changed output controls to two columns. Template Editor's generic silhouette was removed whenever real project media exists.
- **Release evidence:** `QA_RELEASE_EVIDENCE.md` records the QA-01 through QA-12 matrix, capture paths, keyboard/focus/recovery proof, redaction proof, measured theme contrast, performance result, native support matrix, command outcomes, baseline failures, and final **23/24** rendered rubric. The renderer suite passes 302/302, focused main QA passes 33/33, targeted Biome passes, and the production build passes. Repository-wide Biome/typecheck and three unrelated main tests remain the documented dirty-tree baseline rather than a false green claim.